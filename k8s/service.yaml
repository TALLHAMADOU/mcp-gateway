apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  labels:
    app: mcp-gateway
spec:
  type: ClusterIP
  selector:
    app: mcp-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
